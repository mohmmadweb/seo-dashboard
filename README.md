# Lenzit SEO dashboard

The rendered operations dashboard for lenzit.org, published to GitHub Pages.

**This repository holds one generated page and nothing else.** The engine, the
stored history and the working notes live in a separate private repository; only
the page itself is published here.

Regenerated by `seo-os/publish_site.sh` after each run.
